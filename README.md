# PulseDesk

A clean 2-microservice starter:
- **Frontend**: React + Vite + TypeScript (dashboard UI)
- **Backend**: NestJS (API)
- **Database**: not included yet (structure ready to add later)

## Run locally (Docker)
```bash
docker compose up --build
